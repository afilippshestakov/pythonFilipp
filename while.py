# Da A a B divisibili per tre
# L'utente inserisce i numeri A e B (A < B, A minore di B). Stampa i numeri da A a B inclusi, che sono divisibili per tre
# a = int(input("inserisci il numero a: "))
# b = int(input("inserisci il numero b: "))


# while a <= b:
# 	if a % 3 == 0:
# 		print(a)
#	a = a + 1

# Somma dei numeri
# L'utente inserisce numeri finché non inserisce 0. Stampa la somma dei numeri inseriti (0 non deve essere contato).
# somma = 0
# while True:
# 	numero = int(input("inserisci i numero (0 per fermarti): "))
	
# 	if numero == 0:
# 		break
# 	somma = somma + numero
# print(f"Hai inserito come somma: {somma}")

# Massimo
# L'utente inserisce numeri finché non inserisce 0. Stampa il numero massimo inserito (0 non deve essere contato).
max_numero = 0
while True:
	numero = int(input("inserisci i numero (0 per fermarti): "))

	if numero == 0:
		break
	if numero > max_numero:
		max_numero = numero
	if
print(f"il numero massimo e: {max_numero}")